name: Hassfest Validation

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  hassfest:
    name: Hassfest Validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Hassfest
        # Führt Hassfest direkt aus, da die Dateien jetzt im custom_components-Ordner liegen
        uses: "home-assistant/actions/hassfest@master"
